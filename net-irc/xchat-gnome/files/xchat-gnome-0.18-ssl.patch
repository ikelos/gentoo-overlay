diff -uNr xchat-gnome-0.18.old/src/common/server.c xchat-gnome-0.18/src/common/server.c
--- xchat-gnome-0.18.old/src/common/server.c	2008-02-04 20:41:29.000000000 +0000
+++ xchat-gnome-0.18/src/common/server.c	2008-02-04 21:25:01.000000000 +0000
@@ -879,13 +879,13 @@
 		/* it'll be a memory leak, if connection isn't terminated by
 		   server_cleanup() */
 		serv->ssl = _SSL_socket (ctx, serv->sok);
-		if ((err = _SSL_set_verify (ctx, ssl_cb_verify, NULL)))
-		{
-			EMIT_SIGNAL (XP_TE_CONNFAIL, serv->server_session, err, NULL,
-							 NULL, NULL, 0);
-			server_cleanup (serv);	/* ->connecting = FALSE */
-			return;
-		}
+		//if ((err = _SSL_set_verify (ctx, ssl_cb_verify, NULL)))
+		//{
+		//	EMIT_SIGNAL (XP_TE_CONNFAIL, serv->server_session, err, NULL,
+		//					 NULL, NULL, 0);
+		//	server_cleanup (serv);	/* ->connecting = FALSE */
+		//	return;
+		//}
 		/* FIXME: it'll be needed by new servers */
 		/* send(serv->sok, "STLS\r\n", 6, 0); sleep(1); */
 		set_nonblocking (serv->sok);
